export const procesoP = ( it) =>{
    for(let i =0; i < it; i++){
        console.log('iterando...')
    }
    //regresa el numero de iteraciones realizadas 
    return `${it} iteraciones realizadas`;
}